{
  "name": "maxine-walkthrough-worker",
  "version": "1.0.0",
  "description": "Video decomposition worker for Maxine: extracts frames + audio, transcribes, builds interleaved timeline",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.19.2"
  }
}
